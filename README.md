# Golf-Simulator-Start-up-Click-Builder
Visual click sequence builder for Windows. Pick points on any monitor, set wait times and single/double clicks, then run the sequence or export it as a standalone AutoHotkey macro. Per-monitor DPI aware for mixed display scaling. Built by BA Custom Products for golf simulator automation.
